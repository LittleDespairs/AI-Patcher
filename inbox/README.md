# AI Patcher Inbox

This folder is for local, unpublished work bundles.

Generated bundle files should not be committed to the module repository. Codex or another local tool can create:

- `index.json`
- one folder per bundle, such as `my-work/patch.js`

Any Foundry world with AI Patcher enabled can open the inbox and apply those bundles.
